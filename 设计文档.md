# crw（copyReplaceWrite）设计文档

## 1. 项目简介

crw 是一个基于 Bubble Tea 的 TUI 版本管理工具，以 `crw <文件名>` 方式启动。

它的用途是：把目标文件（如 `config.yaml`）与同目录下所有以该文件名作为前缀的备份文件
（如 `config.yaml.bak.20260816_140000`、`config.yaml_back`）组织成一个版本列表，
以 yazi 风格的双栏窗口（左列表 / 右预览）供用户浏览；回车即可用所选备份的内容替换当前文件，
替换前自动为当前内容留档，形成"永不丢失"的版本管理闭环。

## 2. 使用方式

```bash
go build -o crw .
./crw config.yaml          # 打开 /当前目录/config.yaml 的版本列表
./crw /etc/nginx/nginx.conf  # 支持绝对路径
```

启动后：

- 若目标文件或同目录下存在 `目标文件名*` 文件 → 进入 TUI。
- 若两者都不存在 → 打印错误并以非 0 退出码结束。
- 目标文件缺失但存在备份 → 仍进入 TUI，可用备份恢复出目标文件。

## 3. 界面布局

```
┌─────────────────────────────┐ ┌──────────────────────────────────────────────┐
│ ● config.yaml               │ │ config.yaml.bak.20260816_140000  [18%]        │
│   config.yaml.bak.20260816  │ │ ───────────────────────────────────────────── │
│   config.yaml_back          │ │ <文件内容预览，可滚动>                         │
│                             │ │                                               │
└─────────────────────────────┘ └──────────────────────────────────────────────┘
 <状态栏：操作结果 / 错误信息>
 <快捷键提示栏>
```

- 左栏：版本列表。目标文件本身以 `●` 前缀标识并恒在首位；备份按修改时间新 → 旧排列。
- 右栏：内容预览。标题行显示选中文件名与滚动百分比（内容完全可见时隐藏百分比）；
  文本文件按文件名识别语言并做语法高亮（见 5.6）；二进制文件（前 8KB 内出现 NUL 字节）
  显示占位提示，不渲染原始内容。
- 对比模式（F1）：右栏标题变为黄色并显示 `选中文件 ⟷ 母本文件`，内容渲染行级 diff——
  首行黄色统计（`+新增行数 / -删除行数`），新增行绿色（前缀 `+`，行号为当前文件）、
  删除行红色（前缀 `-`，行号为母本）、上下文灰色，便于一眼看清改了什么。
- 焦点面板的边框高亮为蓝色；选中行整行反色高亮。

## 4. 按键说明

| 按键 | 功能 |
|------|------|
| `j` / `k` / `↑` / `↓` | 列表焦点：上下移动光标，预览自动跟随；预览焦点：上下滚动预览 |
| `g` / `G` | 列表焦点：跳转到首 / 末项；预览焦点：滚到顶 / 底 |
| `Tab` | 在列表与预览之间切换焦点 |
| `h` / `←` | 焦点切回列表 |
| `l` / `→` | 焦点切到预览 |
| `Ctrl+D` / `Ctrl+U` | 预览下滚 / 上滚半页 |
| `PgDn` / `PgUp` | 预览下滚 / 上滚一页 |
| `F1` | 切换对比模式：进入时把当前选中文件记为母本，之后预览区显示各文件与母本的行级 diff；再次按 F1 退出（见 5.5） |
| `Enter` | 用选中备份的内容替换目标文件（核心功能，见 5.1；对比模式下语义不变） |
| `c` | 把目标文件当前内容克隆为一份新备份（见 5.2） |
| `d` | 删除选中的备份（弹出确认框） |
| `D` | 删除选中的备份（不确认） |
| `r` | 重新扫描目录，重建版本列表 |
| `q` / `Ctrl+C` / `Esc` | 退出（确认框中 Esc 仅取消确认） |

## 5. 核心行为设计

### 5.1 回车替换（Enter）

流程：

1. 若选中项是目标文件本身：
   - 列表只有一项 → 提示"按 c 克隆备份"，不做任何操作（按约定不产生备份）；
   - 列表有多项 → 提示"这是当前文件本身"，不做任何操作。
2. 否则，读取目标文件当前内容：
   - 目标文件缺失 → 跳过自动备份（缺失不可备份）；
   - 当前内容在**备份列表**（不含目标文件本身）中**不存在**相同项 → 先自动备份当前内容；
   - 已存在相同项 → 不重复备份。
3. 把选中备份的内容写回目标文件（覆盖）。
4. 重新扫描列表，光标停在刚应用的备份项上，状态栏提示结果。

设计要点：

- **自动备份是"安全网"**：把当前内容换回旧版时，当前（较新）状态自动留档，保证可回退。
- **去重**：当前内容若已在备份列表中存在，绝不产生重复文件。
- **单文件场景不产生备份**：仅有一个文件时回车是无意义操作，按约定不备份。

### 5.2 克隆当前内容（c）

流程：

1. 目标文件缺失 → 报错。
2. 在备份列表中查找与当前内容逐字节相同的项：
   - 存在 → 不创建新文件，光标跳转到该备份项，提示"已存在相同内容的备份"；
   - 不存在 → 按 `5.3` 规则创建新备份，光标跳转到新项。
3. 单文件场景下，`c` 是产生第一个备份的途径（回车不会产生备份）。

### 5.3 备份命名规则

```
<目标文件名>.bak.<yyyyMMdd_HHmmss>
示例：config.yaml.bak.20260816_140000
```

- 秒级时间戳保证日常使用唯一；极端情况下（同一秒内并发创建）追加 `_1`、`_2`… 后缀避让，
  绝不允许覆盖已有备份。
- 命名规则决定了版本文件发现规则：**同目录下所有以目标文件名为前缀的文件**均视为备份
  （`config.yaml.bak.20260816`、`config.yaml_back`、`config.yaml.1` 等）。

### 5.4 删除（d / D）

- `d`：弹出居中确认框，`y`/`Enter` 确认删除，`n`/`Esc`/`q` 取消。
- `D`：直接删除。
- 目标文件本身禁止删除（无论 d 还是 D）。

### 5.5 对比模式（F1）

用途：把某个版本作为参考基线（母本），快速查看其它版本相对它改了什么，
无需逐个打开文件目测差异。

流程：

1. 按 `F1`：进入对比模式，**当前光标选中的文件**被记为母本，状态栏提示母本名。
2. 之后移动光标（j/k）切换文件，右侧预览区渲染 `选中文件 ⟷ 母本` 的行级 diff：
   - 首行黄色统计：`对比 <母本名>：+M 行 / -N 行`（新增 / 删除行数）；
   - 逐行着色：新增行绿色（前缀 `+`，行号为当前文件）、删除行红色（前缀 `-`，行号为母本）、
     上下文灰色（前缀空格，行号为母本）；
   - 两文件内容完全相同 → 显示“内容完全相同，无差异”；
   - 任一文件为二进制 → 显示占位提示。
3. 再次按 `F1`：退出对比模式，预览恢复常规内容视图。

设计要点：

- **进入对比模式后其余操作完全不变**：Enter 替换、c 克隆、d/D 删除、r 刷新、
  j/k/g/G 移动、Tab 焦点、滚动照常工作；Enter 替换后预览中的 diff 随内容自动重算。
- **母本被删除时自动退出对比模式**：删除的正是母本时，对比失去参照意义，
  自动退回到常规模式并提示。
- **母本文件被外部删除**：预览显示占位提示（“母本文件已不存在”），不崩溃，按 F1 可退出。
- **diff 算法**：Myers 贪心算法（与 git 同源），行级比较；CRLF 与 LF 行尾差异被忽略，
  避免换行风格噪声淹没真实修改。
- **滚动**：diff 视图同样支持预览区全部滚动操作；渲染文本行数参与“全部可见”判定，
  短 diff 不显示误导性的滚动百分比。
- **语法高亮与对比模式互斥**：对比视图本身已按 diff 语义着色（绿/红/灰），
  不再叠加语法高亮，避免两种配色互相覆盖。

### 5.6 语法高亮

预览区对常规文本文件做语法高亮，效果与 `bat` 命令一致（Monokai 配色、256 色），
但由独立于外部命令的 Go 库实现，无需安装 bat。

实现：

- **语言识别**：按文件名交给 chroma（github.com/alecthomas/chroma/v2，约 250 种语言，
  覆盖 C/C++/Go/Python/Rust/Shell/Makefile/CMake/JSON/YAML/TOML/INI/Markdown/SQL 等）。
  crw 的备份名形如 `config.yaml.bak.20260816_140000`，扩展名是时间戳——识别失败时
  自动剥离 `.bak.<时间戳>`（含同秒冲突的 `_2` 后缀）还原原名再匹配。
  识别不到语言（无扩展名、未知扩展名）时原样渲染纯文本。
- **渲染**：自定义紧凑 formatter 输出 256 色 ANSI：仅“拥有专属颜色”的 token
  （关键字、数字、字符串、注释等）附带转义序列，空格、标点、默认前景色文本原样写出，
  输出量约为标准 TTY256 formatter 的一半。转义序列由 viewport 正常解析，
  滚动、行数、宽度计算均不受影响。
- **性能保护**：超过 64KB 的文件不做高亮（chroma 高亮耗时随大小线性增长，
  实测 64KB ≈ 150ms、1MB ≈ 2.4s，交互式 TUI 必须限制），以纯文本预览；
  高亮结果按 `路径 + 大小 + 修改时间` 缓存（上限 8 项或 2MB，超出整表清空），
  翻列表来回切换同一文件零延迟。
- **失败降级**：分词/渲染出错、无法识别语言时一律原样返回文本——
  高亮是增强功能，任何异常都不得丢内容。

## 6. 架构设计

```
main.go                 入口：解析参数 → backup.List() 扫描 → ui.New() → tea 运行
  │
  ├── internal/backup   版本文件底层能力（无 UI 依赖，可独立测试）
  │     ├── List()              扫描目录，返回排序后的 Entry 列表
  │     ├── IsBackupName()      前缀匹配判断
  │     ├── BackupName()        秒级时间戳命名
  │     ├── UniqueBackupPath()  不冲突路径（_1/_2 避让）
  │     ├── ReadContent()       读文件（缺失返回 nil，不视为错误）
  │     ├── WriteContent()      写文件
  │     ├── Delete()            删除文件
  │     ├── FindIdenticalBackup()  内容去重查找（排除目标文件本身）
  │     └── diff.go             Myers 行级 diff：SplitLines() / Diff()
  │
  └── internal/ui       Bubble Tea 界面
        ├── model.go     Model 结构、New()、Init()
        ├── update.go    Update() 消息分发、handleKey() 按键处理、layout() 布局
        ├── actions.go   业务动作：performReplace / performCopy / askDelete /
        │                performDelete / refresh / loadPreview / toggleCompare /
        │                loadDiffPreview / renderDiff
        ├── highlight.go 语法高亮：highlightSource()（chroma 分词 + 紧凑 256 色
        │                formatter）、lexerForName()（备份名剥离）、highlightPreview()
        │                （Model 级结果缓存）
        └── view.go      渲染：header / listView / previewView / confirmBox / footer
```

状态模型：

- `mode`：`modeNormal`（正常浏览）/ `modeConfirmDelete`（删除确认）。
- `focus`：`focusList` / `focusPreview`，决定 j/k 的语义（移动光标或滚动预览）。
- `compareMode`：对比模式开关（F1 切换）；`compareBase` 记录进入时的母本条目。
  对比模式与删除确认框正交：确认框优先拦截按键，确认框内 F1 无效。
- 全部方法使用指针接收者：避免复制内部含锁的 viewport.Model（go vet copylocks）。

## 7. 关键流程时序

### 7.1 回车替换

```
用户按 Enter
  └─ 选中项是目标文件本身? ──是──> 提示，结束（不产生备份）
  └─ 否
      ├─ 目标文件存在?
      │    ├─ 是：当前内容在备份列表有相同项?
      │    │     ├─ 否 → 创建 <名>.bak.<时间戳> 备份当前内容
      │    │     └─ 是 → 跳过
      │    └─ 否 → 跳过备份
      ├─ 选中备份内容 → 写回目标文件
      └─ 刷新列表，光标停在选中备份，提示结果
```

### 7.2 克隆（c）

```
用户按 c
  └─ 目标文件存在?
      ├─ 否 → 报错
      └─ 是
          ├─ 备份列表有相同内容项?
          │    ├─ 是 → 光标跳转到该项，不创建文件
          │    └─ 否 → 创建 <名>.bak.<时间戳>，光标跳转到新项
          └─ 提示结果
```

## 8. 边界情况与异常处理

| 场景 | 行为 |
|------|------|
| 目标文件与备份都不存在 | 启动即报错退出（非 0 退出码） |
| 目标文件缺失但备份存在 | 进入 TUI；Enter 用备份恢复出目标文件；c 报错；替换不自动备份 |
| 选中目标文件本身按 Enter | 不操作；单文件时提示按 c |
| 仅一个文件时按 Enter | 不产生备份 |
| 当前内容已在备份列表 | 替换 / 克隆均不产生重复备份 |
| 删除目标文件本身 | 拒绝（d 与 D 均拦截） |
| 同一秒内并发创建备份 | `_1`、`_2` 后缀避让，绝不覆盖 |
| 目录不可读 / 文件不可写 | 状态栏红色错误提示，不崩溃 |
| 二进制文件 | 预览显示占位提示；对比模式下一方为二进制同样只显示占位 |
| 无法识别语言（无扩展名/未知扩展名） | 预览原样显示纯文本，不产生高亮 |
| 文件超过 64KB | 不做语法高亮，纯文本预览（避免切换卡顿） |
| 高亮分词/渲染出错 | 降级为纯文本，绝不丢内容 |
| 终端过小（< 60x12） | 提示放大窗口 |
| 外部新增/删除备份文件 | `r` 键或任意操作后自动重建列表 |
| 对比模式下母本被 `d`/`D` 删除 | 自动退出对比模式并提示 |
| 对比模式下母本被外部删除 | 预览显示“母本文件已不存在”占位，不崩溃；按 F1 退出 |
| 对比模式下文件内容完全相同 | 预览显示“内容完全相同，无差异” |
| CRLF 与 LF 混排的相同内容 | diff 判为无差异（行尾 `\r` 剥离后再比较） |

## 9. 测试策略

- `internal/backup`：文件发现与排序、命名格式、路径避让、内容读写、去重查找、缺失文件语义。
  新增 diff.go 专项：Myers 算法行为（追加 / 删除 / 修改 / 首行修改 / 空文件 / 大文件）、
  CRLF 归一化、SplitLines 边界。
- `internal/ui`：直接对 Model.Update 注入按键消息，验证完整业务流程：
  替换+自动备份、去重跳过备份、单文件回车、c 克隆/跳转、d 确认/取消/D 直删、
  目标文件删除拦截、刷新重建列表、预览跟随、焦点切换。
  新增对比模式专项：F1 进入/退出与母本记录、diff 预览内容（+/- 与统计）、
  相同内容提示、对比模式下 Enter 语义不变、母本被删除自动退出、母本外部删除占位提示。
- 新增语法高亮专项：C 源码产生 ANSI 且剥离转义后内容不变、备份名（含 `_2` 冲突后缀）
  剥离时间戳后识别语言、未知/无扩展名不高亮、超 64KB 不高亮、空文件、关键字确实着色、
  lexerForName 识别矩阵、Model 级缓存命中与失效（大小/时间变化）。
- 端到端：在 tmux PTY 中运行真实二进制，逐键验证渲染与文件系统效果。

```bash
go vet ./...
go test ./...
go build -o crw .
```

## 10. 设计决策记录

| 决策 | 理由 |
|------|------|
| 备份匹配采用"前缀匹配"而非严格 `.*` 点分 | 兼容 `.bak.时间戳`、`_back`、`.1` 等多种命名风格，规则简单可解释 |
| 目标文件固定列表首位，备份按 mtime 新→旧 | 最新版本最常用，排序直观稳定 |
| 替换成功后留在界面而非退出 | 可立即预览结果、继续替换其他版本或按 r 刷新；q 随时退出 |
| 焦点切换（Tab）与方向键（h/l）并存 | 兼顾 vim 习惯与 yazi 方向键习惯 |
| 自动备份仅针对"备份列表"，不比较目标文件本身 | 目标文件就是当前内容，比较它会让自动备份永远不触发 |
| 删除需确认（d）/ 不确认（D）双键 | 与 yazi 的 d 删除确认习惯一致，D 提供熟练用户快路径 |
| 对比模式用 `F1` 进入/退出（toggle） | 单键开关便于快速进入/退出；进入时选中项即母本，无需额外子模式按键 |
| 对比模式独立于 `mode` 枚举（`compareMode` 布尔） | 与删除确认框正交：确认框拦截优先级不变，对比状态不干扰既有模式机 |
| diff 用 Myers 贪心算法 | 与 git 同源的最优编辑距离算法，O((N+M)D) 时间 O(N+M) 空间；行级 diff 对配置文件足够直观 |
| diff 剥离行尾 `\r` | CRLF/LF 混排不产生噪声差异，专注真实内容修改 |
| 对比模式渲染带行号 | 前缀 `+/−` 旁标注各自文件行号，改动定位可直接对应原文 |
| 语法高亮用 chroma 库而非调用系统 bat | 纯 Go 库（约 250 种语言）编译进二进制，零外部依赖；bat 是 Rust 程序，安装与否不可控，且输出需再解析 |
| 自定义紧凑 formatter 而非标准 TTY256 | 只给“非默认前景色”的 token 输出转义，输出量减半、内存更省；显示效果与 TTY256 一致 |
| 高亮大小上限 64KB | chroma 高亮耗时随文件大小线性增长（实测 64KB≈150ms、1MB≈2.4s）；64KB 覆盖配置文件与常见源码，交互不卡顿 |
| 高亮结果按 路径+大小+修改时间 缓存 | 翻列表反复切换同一文件零延迟；缓存上限 8 项/2MB，内存有界 |
| 备份名先剥离 `.bak.<时间戳>` 再识别语言 | 备份文件扩展名是时间戳，直接匹配必失败；剥离后还原原名（含 `_2` 冲突后缀） |
| 无法识别语言时降级纯文本 | 无扩展名/未知扩展名文件不猜语言，避免误着色误导阅读 |
